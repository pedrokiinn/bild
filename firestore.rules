rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite acesso total apenas para o seu usuário específico
      allow read, write: if request.auth.uid == 'keennlemariem@gmail.com';
    }
  }
}
