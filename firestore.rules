rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // PERIGO: Permite que qualquer um leia e escreva. Apenas para testes.
      allow read, write: if true;
    }
  }
}